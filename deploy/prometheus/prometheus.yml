global:
  scrape_interval: 15s
  scrape_timeout: 10s
  evaluation_interval: 15s
  external_labels:
    service: proxyharbor

rule_files:
  - alerts.yml

scrape_configs:
  - job_name: proxyharbor
    metrics_path: /metrics
    scheme: http
    static_configs:
      - targets: [api:8080]
