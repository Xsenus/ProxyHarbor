<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\ProxyHarbor.Infrastructure\ProxyHarbor.Infrastructure.csproj" />
  </ItemGroup>
  <ItemGroup>
    <!-- Restore напрямую использует binary COPY API, поэтому зависимость не оставляем транзитивной. -->
    <PackageReference Include="Npgsql" Version="10.0.3" />
  </ItemGroup>
</Project>
